purge_keep_days: 7
purge_interval: 1
include:
  domains:
    - switch
    - lock
    - cover
    - light
    - sensor
exclude:
  domains:
    - weblink
    - updater
    - camera
  entities:
    - sensor.family_last_changed
    - sensor.occupancy_last_changed