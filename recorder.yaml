purge_keep_days: 7
purge_interval: 1
include:
  domains:
    - switch
    - lock
    - cover
    - light
    - sensor
    - binary_sensor
    - weather
    - image_processing
    - input_select
exclude:
  domains:
    - weblink
    - updater
    - camera